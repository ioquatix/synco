method: rsync-snapshot-pull --compress --stats
master: server.koyoko

server.koyoko:
  host: koyoko.oriontransfer.org
  shell:
    command: ssh
    user: samuel

server.backup:
  after:
    - %prune --default-policy
  root: "/koyoko-srv-git-rotations"

directory.git:
  path: srv/git

